# Bri4ka - план развития

Ниже перечислены планируемые к реализации функции, разделенные по категориям:

# Свойства и параметры

Загрузчик свойств из Revit ФОП

Поиск неиспользуемых свойств

Поиск и объединение дубликатов свойств (объединение нужен доступ к редактированию выражений у свойств объектов (?)

~~Мультисвойства (несколько выбранных обьектов)~~ ModPlus Быстрые свойства

# Стили и данные проекта

Загрузчик материалов, стилей из файла/другого проекта

Поиск неиспользуемых стилей\материалов

Внешняя бд семейств с поиском и карточками, плюс stdl, вставка в модель (Renga PRO для вставки STDL), немодальное окно

# Оформление

Создание различных таблиц линиями и текстом

Создание ведомости листов

# Измерения и статистика

+Уклоны кровли

Квартирография, формирование сводных контуров помещений

+Статистика граней

Статистика обьектов по уровням (втч поиск пустых уровней)

Расчет потерь в инженерных системах суммарный (постановка задачи)

-Вывести метаданные файла (чтение RNP)

# Настройки

~~Настройка единиц расчетных параметров~~ (для чего?)

~~Api explorer (net fr)~~ Замороченно. Там всё на методах в основном

# Утилиты

Задание на отверстия (при проверке на коллизии нужно вычислять коллизирующую точку)

Избранные сборки (панель слева закрепленная), перечень всех сборок с настройкой отображения. При выборе в таблице -- вставка в модель

Копирование элементов на уровни или на высоту с шагом (с автоматически созданием новых уровней или без них)

# Навигация

Видовой куб (внешнее окно)

Текущие координаты и положение камеры (закрепленное окно снизу слева экрана), перехват камеры

Быстрые геометрические свойства (закрепленное окно снизу слева экрана), перехват текущего выбора Пользователя, если геометрия -- отражение координат